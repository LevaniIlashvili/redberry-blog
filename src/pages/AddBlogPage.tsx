import styled from "styled-components";

const AddBlogPage = () => {
  return <Wrapper>add blog</Wrapper>;
};

const Wrapper = styled.main`
  background-color: var(--page-background);
  min-height: calc(100vh - 8rem);
`;

export default AddBlogPage;
